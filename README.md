# JP Stock Screener

日本株のテクニカル指標を毎営業日自動取得・計算し、静的ダッシュボードで閲覧できる個人用スクリーニングシステム。外部の有料APIは使わず、GitHub Actions の Cron と GitHub Pages のみで完結します。

## 構成

| ファイル | 役割 |
|---|---|
| `.github/workflows/update_data.yml` | 平日 11:30 JST（前場引け）に自動実行される GitHub Actions ワークフロー |
| `src/update_stocks.py` | yfinance で株価（過去約2年分）を取得し、RSI・MACD・移動平均トレンド・ボリンジャーバンド・出来高などを計算して `data/stocks.json` に保存 |
| `src/index.html` | Tailwind CSS (CDN) + バニラJS のビルド不要ダッシュボード（ダークモード） |
| `data/stocks.json` | 計算済みデータ（Actions が自動更新） |

## セットアップ

1. リポジトリの **Settings → Pages** で、Source を「Deploy from a branch」、ブランチをメインブランチ（フォルダは `/ (root)`）に設定
2. **Actions** タブから `Update Stock Data` ワークフローを手動実行（workflow_dispatch）して初回データを生成
3. `https://<ユーザー名>.github.io/<リポジトリ名>/src/` にアクセス

以降は平日 11:30 JST に自動でデータが更新されます。

## 銘柄の追加

`src/update_stocks.py` の `STOCKS` 配列に1行追加するだけです。

```python
{"code": "XXXX.T", "name": "銘柄名", "market": "市場名"},
```

## 表示指標

- 現在値（終値）・前日比（騰落率 %）
- RSI (14日, Wilder 平滑化)
- 25日移動平均乖離率 (%)
- MACD ヒストグラム
- ボリンジャーバンド %B（20日, ±2σ）
- 出来高（20日平均比）
- **総合テクニカルスコア**: RSI・MACD・移動平均トレンド（25日/75日）・ボリンジャーバンド・出来高の5指標を機械的に合成した客観的な数値（-5〜+5）。「強気シグナル優勢／やや強気／中立／やや弱気／弱気シグナル優勢」の5段階で表示。
- **統計的上昇確率（参考）**: 同一銘柄の過去約2年間で、現在と同じ総合テクニカルスコアのバケットが発生した際に5営業日後の株価が上昇していた頻度（%）。該当件数（サンプル数 n）が5件未満の場合は「参考不可」として非表示。

## 免責

本システムはテクニカル指標の数値を客観的に表示するものであり、投資勧誘や売買推奨を行うものではありません。「総合テクニカルスコア」「統計的上昇確率」はいずれも過去の値動きを機械的に集計した参考値であり、将来の値動きを予測・保証するものではありません。特に統計的上昇確率は個別銘柄ごとの過去データのみに基づく単純集計のため、サンプル数が少ない場合は統計的な信頼性が低い点に留意してください。データは Yahoo Finance 由来で遅延・欠損を含む場合があります。投資判断はご自身の責任で行ってください。個人利用限定。
